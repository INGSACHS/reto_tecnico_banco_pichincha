{
  "id": "${idMascota}",
  "category": {
    "id": "${idCategoria}",
    "name": "${nombreCategoria}"
  },
  "name": "${nombreMascota}",
  "photoUrls": [
    "${urlFotoMascota}"
  ],
  "status": "${estadoMascota}"
}